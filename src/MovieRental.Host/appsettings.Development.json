{
  "Database": {
    "// UseMigrations": "false rebuilds the database whenever the model changes — fine until you care about the data in it. Run scripts\\migrations.ps1, then set this to true.",
    "UseMigrations": false
  },
  "Frontend": {
    "// UseDevServer": "Set to true while `npm run dev` is running in /client for hot reload.",
    "UseDevServer": false
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "MovieRental": "Debug"
    }
  }
}
